```
python3 -m venv .
source bin/activate

# You shouldn't need these, but I did?
# pip install --upgrade pip setuptools wheel
# pip install --upgrade numpy
# pip install numpy --only-binary :all:


pip install spacy
```
